export function SiteFooter() {
  return (
    <footer className="relative z-10 border-t border-line">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-4 px-4 py-8 text-sm text-muted sm:px-6 lg:px-8 md:flex-row md:items-center md:justify-between">
        <p>FrameFlow brings movies and series into one polished watch hub.</p>
        <p>
          Browse, jump into playback, and pick up where you left off across movies
          and TV.
        </p>
      </div>
    </footer>
  );
}
